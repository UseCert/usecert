# Addresses allowed through while maintenance is on, so the thing being fixed can be
# tested from the same browser that is looking at the maintenance page.
# The file must exist even when empty or nginx refuses to start.
geo $maint_bypass {
    default 0;
    include /etc/nginx/maintenance/allow.conf;
}
